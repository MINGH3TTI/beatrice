type Actuator {
  fan: Boolean
  nebulizer: Boolean
  heater: Boolean
  lamp: Boolean
}

type Limits {
  tempMin: Float
  tempMax: Float
  humidityMin: Float
  humidityMax: Float
  noiseMax: Float
}


type Enclosure {
  id: ID!
  name: String!
  speciesId: String
  photoUrl: String
  lastReadings: Variant
  limits: Limits
  actuators: Actuator
  status: String
  operatorIds: [String]
}

type ToggleActuatorResult {
  success: Boolean!
  message: String
}

input LimitsInput {
  tempMin: Float
  tempMax: Float
  humidityMin: Float
  humidityMax: Float
  noiseMax: Float
}

input CreateEnclosureInput {
  name: String!
  speciesId: String
  photoUrl: String
  status: String
  limits: LimitsInput
  tempMin: Float
  tempMax: Float
  humidityMin: Float
  humidityMax: Float
  noiseMax: Float
}

input UpdateEnclosureInput {
  name: String
  speciesId: String
  photoUrl: String
  status: String
  limits: LimitsInput
  tempMin: Float
  tempMax: Float
  humidityMin: Float
  humidityMax: Float
  noiseMax: Float
}

type Query {
  enclosures(operatorId: ID): [Enclosure]
  enclosure(id: ID!): Enclosure
  enclosureLimits(id: ID!): Limits
}

type Mutation {
  updateActuatorStateFromEsp32(enclosureId: ID!, actuatorType: String!, state: Boolean!): ToggleActuatorResult
  createEnclosure(input: CreateEnclosureInput!): Enclosure
  updateEnclosure(id: ID!, input: UpdateEnclosureInput!): Enclosure
  deleteEnclosure(id: ID!): DeleteResult
  seedEnclosures: [Enclosure]
}
